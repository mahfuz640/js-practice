function add(num1, num2) {
    var total = num1 + num2
    return total
}
var result = add(45, 5) //add nam ar akta function create kore pore amra abar call kortese thn chaile jog biyog gun bhag shob korte pari

console.log(result)

//function diye array ar 
function list(n) {
    for (var i = 0; i < n.length; i++) {
        var element = n[i]
    }
}
list = ([45, 666, 522])
console.log(list)